require 'spec_helper'

describe SpreePaygreen do
  it 'has a version number' do
    expect(SpreePaygreen::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
